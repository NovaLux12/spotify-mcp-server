# 2026-08-27 — Live sweep rig for full 224-tool coverage (resumable)

## Pick-up point (session resume) <!-- project: github.com/novalux12/spotify-mcp-server -->

Everything is in place for the quota-paced live sweep of the full tool surface.
**To resume:** `cd ~/.openclaw/projects/d3e4a3d09c243613/spotify-mcp-server && npm run build && npm run sweep:loop`

- Plan + rationale: `memory/live-sweep-plan.md`
- Runner: `scripts/live-gauntlet.mjs` (now supports `--batch=N`, `--resume=FILE`, `--report=FILE`, GATED classification + GATE_SNIFF, SWEEP_COMPLETE fast path) — patched 00:10 BST
- Loop: `scripts/sweep-loop.sh` (BATCH=40, INTERVAL=1800 default) + npm scripts `sweep` / `sweep:loop`
- Cumulative report: `memory/live-sweep-report.json` (does not exist yet — first run creates it)
- `.env` in repo root (gitignored) holds SPOTIFY_CLIENT_ID from openclaw.json so token auto-refresh works; `~/.spotify-mcp/tokens.json` has a live refresh token.

## Overnight 26→27 Aug probe results (context for the sweep) <!-- project: github.com/novalux12/spotify-mcp-server -->

- Live gauntlet (uncapped) quota-walled after ~25 calls: 11 PASS, 12 SKIP(mutating), get_album 120s timeout. July-2026 per-dev-account quota: first ~45 calls then Retry-After cascade — batch spacing is mandatory.
- Edge probe (37 endpoints, `memory/edge-probe-2026-08-26.json`): 13 ALIVE (incl. undocumented `/me/library/contains` → 200), 3 DEAD (recommendations, genre-seeds, me/notifications), 2 GONE 410 (me/apps, me/chapters), 16 GATED 403 (browse categories/new-releases, markets, artist top-tracks, users-by-id(+playlists), ALL documented contains variants), 3 wrong-method 405 (player/play, next, volume).
- Tool-level: gated four explain 403 gracefully; browse family returns raw `Forbidden` (issue #328).
- Count drift: `tools/list` = 224 vs docs 212 (issue #327).

## Issues (template-conforming, labelled) <!-- project: github.com/novalux12/spotify-mcp-server -->

- #327 bug — count drift 224 vs 212
- #328 bug — browse family raw Forbidden
- #329 enhancement — app-registration-gated disclosure
- #330 enhancement — gauntlet GATED/batch/resume (implemented)

Probe scripts kept: `scripts/edge-probe.mjs`, `scripts/tool-gate-check.mjs`, `scripts/contains-check.mjs` (all read-only).