# 2026-08-26 — Spotify MCP marathon + swarm orchestration

## Swarm dispatch (30 issues → 14 PRs → 1.23.0 prep) <!-- project: github.com/novalux12/spotify-mcp-server -->
- Dispatched 6 initial streams A-F covering 30 open issues (auth/config/doctor, freshness/quota, safety/receipts, portability, library/discovery, queue/episode/UX) → PRs #243-248. Added quota fixes #254-255 (#242 twins). All on `opencode-go/muse-spark-1.2-contributor` high thinking. <!-- project: github.com/novalux12/spotify-mcp-server -->
- Exhaustive sweep reframed to max quantity (not just beat 154): 4 domains catalog/playback/playlists/portability → 85 raw candidates → audit dedup to 73 unique → logged 60 issues #256-318 (1s rate-limit). Covers duplicate vs #229/#220/#224, SPEC §9 deprecated check. <!-- project: github.com/novalux12/spotify-mcp-server -->
- 5 fix streams for exhaust (catalog 14, playback 15, playlists 18, portability 14, misc 10) → PRs #319-323. Remnants gap 14 (#265-267,301-307,311-314) → PR #324 `fix/exhaust-remnants`. Total 95/95 Closes coverage. <!-- project: github.com/novalux12/spotify-mcp-server -->
- Independent review-fix pass: *** groups (A-E) adversarial 100/100 scoring, fixes pushed (db42178, 4801efd, c4e6096, fa9de90, 262fe0a etc), all `tsc` clean, suites 841-916 green. Merges held until GO, then merged 14 in order to `f2eee0f` → 212 tools (was 154). Inventory failures 5 → fixed in #326 (4770f08) grounding README/SPEC/ARCH on 212/14/17. Remaining PR #325 release-please 1.23.0 held; 49 issues remain open due to squash not carrying Closes footer (needs bulk-close). <!-- project: github.com/novalux12/spotify-mcp-server -->

## Tool count monotonicity <!-- project: github.com/novalux12/spotify-mcp-server -->
- Durable rule: each release must ship more tools than previous (1.22.0 = 154). Logged to USER.md. Current main 212 ensures 1.23.0 monotonic. Feature scouts target ~60-80 candidates to exceed comfortably. <!-- project: github.com/novalux12/spotify-mcp-server -->

## Quota audit findings (1+N lurkers) <!-- project: github.com/novalux12/spotify-mcp-server -->
- Audited fan-out walks: `show_new_episodes` (1+M, no budget), `check_artist_releases/artist_release_digest` (N watchlist unbounded), plus P2 `library_coverage_report`/`find_duplicate_playlists`, P3 `backup_library`. Fixed with `max_shows`/`max_artists` + `dry_run` cost estimate + 429 partial recovery pattern mirroring #242 (budget = min(SPOTIFY_MCP_FRESHNESS_BUDGET, FETCH_ALL_CAP)). Report in `audit-quota-lurkers.md`. <!-- project: github.com/novalux12/spotify-mcp-server -->

## Swarm orchestration lessons
- Concurrency caps: `sessions_spawn` 5, `models.maxConcurrent` 4 — queued F after B, not opencode.go limit. Pin subagents explicitly to parent model+thinking; don't rely on inheritance. <!-- project: github.com/novalux12/spotify-mcp-server -->
- Squash merge loses `Closes #` auto-close — need bulk `gh issue close` after merges or use merge commit with footer.

## Docs grounding for 1.23.0 <!-- project: github.com/novalux12/spotify-mcp-server -->
- Grounded counts: 212 registrations (210 unique, 43 modules), 14 prompts (+music_briefing/morning_briefing/weekly_digest/crate_digging), 17 resources (16 URIs +7 templates). Fixed smoke tests (exposes prompts, resource URIs, market_availability harness). <!-- project: github.com/novalux12/spotify-mcp-server -->

---
## Wrap-up 23:10 BST — merges + 1.23.0 release

_Session: wrap-up 22:08–23:10 BST | Late wrap-up (after 22:00, filing under today per v5.3)_

### TL;DR
- Merged 14 PRs in order to `0e15647` (212 tools), fixed 5 inventory failures, grounded docs, bulk-closed 49 shipped issues, tagged `v1.23.0` (21:46 UTC), npm 1.23.0 published via `NPM_TOKEN` fallback after OIDC provenance failure.

### Decisions
- Squash merges lost `Closes #` footer → bulk `gh issue close --reason completed` with `Shipped in 1.23.0 (4770f08)` traceability, attributed to NovaLux12 repo not Jack. Config for next time: ensure squash body includes footer or use merge commit.
- Tag push via `GITHUB_TOKEN` suppressed `publish.yml` (0s failures on push tags) → published locally via `npm publish --access public` with bypass_2fa token after `npm publish --provenance` failed (`provider: null`).

### Gotchas / surprises
- Exhaustive 60-issue batch: all 60 already in `main` after 13 merges, but GitHub still showed 49 open due to missing Closes — required manual close loop (0.4s sleep).
- `npm publish --provenance` fails locally (needs OIDC); fallback without flag succeeded via `NPM_TOKEN`.
- `publish.yml` `workflow_dispatch` on tag ref `v1.23.0` returned 422 — dispatch expects branch on default, not tag.

### Personality / identity
- No tone correction; held merge order and release hygiene as requested. No identity shift.

### Open threads
- Release-please now at `0e15647` 1.23.0 — next cycle starts from 0 open issues/0 open PRs. Monitor `publish.yml` OIDC vs token fallback for 1.24.

### Lessons (durable)
- Routed to `workspace/learning/corrections.md` (squash Closes loss, provenance fallback, exhaustive quantity framing).


## Live API-surface probe (00:00-00:10, 2026-08-27 BST) <!-- project: github.com/novalux12/spotify-mcp-server -->
- Ran live gauntlet vs Jack's real account: quota-walled after ~25 calls (11 PASS incl get_me/search/get_track/get_artist/get_artist_albums/get_devices/get_user_playlists; 12 SKIP mutating; get_album 120s quota timeout). Matches documented July-2026 per-developer-account quota behaviour — full 212 sweep needs spacing across the day.
- Edge probe (37 endpoints, scripts/edge-probe.mjs → memory/edge-probe-2026-08-26.json) on Jack's key:
  - 13 ALIVE: player/currently-playing/queue/devices/recently-played/top-artists/following/saved-tracks/albums/shows/episodes/audiobooks, search+include_external, **undocumented /me/library/contains → 200** (restore.ts already uses it — the workaround is real)
  - DEAD 404: recommendations, genre-seeds, me/notifications | 410 Gone: me/apps, me/chapters
  - GATED 403 Forbidden (app-registration class, NOT scope): audio-features/analysis, related-artists, featured-playlists, markets, artist-top-tracks, users/{id}(/playlists), browse/new-releases + categories(+playlists), ALL documented /me/*/contains + following/contains + playlists/{id}/followers/contains
  - Tool-level (scripts/tool-gate-check.mjs): get_available_markets/get_artist_top_tracks/get_user_profile/get_user_playlists_by_id return graceful "removed/app-registration" messages; get_categories/get_category_playlists return RAW Forbidden (inconsistent — no graceful wrapper).
- Count drift: tools/list on real key = **224 tools registered vs 212 documented** — README/SPEC/CHANGELOG reground needed.
- 4 issues filed (count drift, browse graceful-403, gated-surface disclosure, gauntlet REMOVED-set expansion). Probe scripts kept in scripts/ (edge-probe, tool-gate-check, contains-check).
